# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Leonardo Capossio - bard0 design

# VexRiscv-full and the whole ddr_ui domain run at a single 100 MHz clock
# (see hardware/scripts/build_arty_a7_vex.py). MIG is reconfigured to produce
# a 100 MHz ui_clk so there is no CPU->DMA CDC.
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=100000000
